package com._8functionalInterface;
// a functional interface is an interface
// that specifies only ONE empty/abstract method
public interface CalculateD
{	// takes in int x, returns an instance of Myclass : returns an object (which is int myVal)
	MyClass method (int x);
}
