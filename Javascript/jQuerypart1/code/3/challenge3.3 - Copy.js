$(document).ready(function() {
    var message = $('<span>Call 1-555-jquery-air to book this tour</span>');
    // $('.book').before(message); // challenge 3.4
    // $('.usa').append(message);  // challenge 3.5
    $('.book').remove(); // challenge 3.6
});